{
  "name": "twilio-dashboard-bughub",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "deploy:worker": "npm run deploy --prefix cf-backend",
    "deploy:pages": "npm run build --prefix frontend && wrangler pages deploy frontend/dist --project-name=bughub-dashboard",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wrangler": "^4.95.0"
  }
}
